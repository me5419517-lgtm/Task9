import 'package:flutter/material.dart';
class My_Column extends StatelessWidget {
  final String number;
  final String title;
  const My_Column(this.number,this.title);

  @override
  Widget build(BuildContext context) {
return Column(
  mainAxisAlignment: MainAxisAlignment.center,
  children: [
    Text(number,
      style: TextStyle(
          color: Colors.white,
          fontSize: 23,
          fontWeight: FontWeight.normal
      ),
    ),
    SizedBox(height: 5,),
    Text(title,
      style: TextStyle(
          color: Colors.white60,
          fontSize: 15,
          fontWeight: FontWeight.normal
      ),)
  ],
);
  }
}
